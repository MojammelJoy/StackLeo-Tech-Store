// StackLeo — shared base Tailwind CSS configuration, extended by apps/web
// and apps/admin. This is a placeholder establishing the file's role;
// brand tokens (colors, spacing, typography) are added during implementation.

/** @type {import('tailwindcss').Config} */
export default {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};
