// Thin re-export of the root tsconfig.base.json, published under
// @stackleo/config for workspaces that prefer resolving shared config
// through the package rather than a relative root path.
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../../../tsconfig.base.json"
}
